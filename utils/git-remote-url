#!/usr/bin/env bash

host_package_ident="$(host-package-ident "${1:?}")"
if [[ $host_package_ident == *.localhost*/*/* ]]; then # for now we can be pretty sure that only .localhost domains are http
  echo "http://$host_package_ident"
else
  echo "https://$host_package_ident"
fi